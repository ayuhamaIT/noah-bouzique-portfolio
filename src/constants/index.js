import translations from './translations';

const fr = translations.fr || {};

export const navLinks = fr.navLinks || [];
export const counterItems = fr.counterItems || [];
export const abilities = fr.abilities || [];
export const techStackImgs = fr.techStackImgs || [];
export const techStackIcons = fr.techStackIcons || [];
export const expCards = fr.expCards || [];
export const expLogos = fr.expLogos || [];
export const socialImgs = fr.socialImgs || [];

export default fr;
